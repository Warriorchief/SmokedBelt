<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <style>
    *{font-size: 22px}
    .output{
      border:3px solid purple;
      width:500px;
      border:4px solid brown;
      padding:8px;
    }
    .clickables{
      position:absolute; top:30px;right:30px;
      font-size:20px;
    }
    </style>
  </head>
  <body>
    <br><br>
    <div class="output">
      <a href="/show_user/<%=@writer.id%>"><%=@writer.name%></a> suggests:<br>
      <p style="border:2px solid darkgreen; font-style:oblique; width:350px;"><%=@this_idea.content%></p>
    </div><br><br>

    <h2>People who liked this post:</h2><hr>
    <table class="table table-bordered">
      <thead>
        <tr>
          <th>Alias</th>
          <th>Name</th>
        </tr>
      </thead>
      <tbody><br>
        <% for u in @unique_likers %>
          <tr>
            <td><a href="/show_user/<%=u.id%>"><%=u.alias%>  </a></td>
            <td><%=u.name%></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <div class="clickables">
      <a href="/logout">Log Out</a><br><br>
      <a href="/profile/<%=@user.id%>">All Ideas</a>
    </div>

  </body>
</html>
